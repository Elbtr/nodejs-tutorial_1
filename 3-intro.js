// ComminJS, every file is moduel (by default)

const first = "Hasael";
const second = "Butar Butar";

const sayHi = (name) => {
  console.log(`Hello my name is ${name}`);
};

sayHi("Monica Datubara");
sayHi(first + second);
